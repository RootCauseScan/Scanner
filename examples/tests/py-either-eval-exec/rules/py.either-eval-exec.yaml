rules:
- id: py.either-eval-exec
  message: Avoid dynamic eval/exec
  severity: ERROR
  patterns:
    - pattern-either:
        - pattern: eval($X)
        - pattern: exec($X)
    - metavariable-regex:
        metavariable: $X
        regex: '^[A-Za-z_(].*'
  languages: [python]
