rules:
  - id: examples.metavariable-pattern
    message: Print of user input
    severity: LOW
    patterns:
      - pattern: print($X)
      - metavariable-pattern:
          metavariable: $X
          pattern: input(...)
